import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//takes everything from the app.js and renders it to the html(root)
ReactDOM.render(<App />, document.getElementById('root'));
